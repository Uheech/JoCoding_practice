1. **index.tsx UI 및 로직 개선 (V6.0)**:
   - API 키 입력 필드에 `id` 및 `name` 속성을 추가하여 브라우저 경고를 제거합니다.
   - `volume` 상태 변수를 추가하고, `AudioWorklet`에서 받은 오디오 데이터의 평균 진폭을 계산하여 실시간으로 업데이트합니다.
   - 화면 하단에 현재 볼륨 크기를 보여주는 가로형 막대 그래프(Volume Meter)를 추가합니다.
   - 버전 표시를 `[V6.0 - VISUAL FEEDBACK]`으로 업데이트합니다.
2. **배포 및 최종 확인**:
   - GitHub 푸시 및 Vercel 배포 완료 후, 마이크에 대고 말을 할 때 초록색 볼륨 바가 실시간으로 움직이는지 확인하도록 안내합니다.
