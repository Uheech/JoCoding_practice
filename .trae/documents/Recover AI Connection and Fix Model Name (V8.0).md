1. **index.tsx 모델 유지 및 디버깅 강화 (V8.0)**:

   * 모델 이름 `gemini-2.5-flash-native-audio-preview-12-2025`를 고수합니다.

   * `initSession`의 모든 콜백(`onopen`, `onmessage`, `onerror`, `onclose`)에 상세한 `console.log`를 추가하여 서버와의 통신 내용을 낱낱이 기록합니다.

   * `onmessage`에서 오디오 데이터 외에 텍스트나 시스템 메시지가 오는지 확인하기 위해 전체 메시지 객체를 출력합니다.

   * `this.updateError`를 사용하여 세션 연결 실패 시 그 원인을 사용자 화면에 직접 노출합니다.

   * 데이터 전송 시 세션 상태를 체크하여 안정성을 높입니다.
2. **UI 및 버전 업데이트**:

   * 버전 태그를 `[V8.0 - DEEP DEBUG]`로 업데이트합니다.

   * 화면 상단 에러 표시줄의 시인성을 높입니다.
3. **배포 및 확인**:

   * GitHub 푸시 후 배포가 완료되면, 화면에 뜨는 에러 메시지나 콘솔의 통신 로그를 바탕으로 다음 조치를 결정합니다.

