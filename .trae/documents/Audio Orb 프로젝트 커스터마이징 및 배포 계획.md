## GitHub 업로드 즉시 실행

1.  **Git 초기화 및 파일 정리** (`git init`, `rm dist/.gitignore`)
2.  **커밋 생성** (`git add .`, `git commit`)
3.  **원격 연결 및 푸시** (`git remote add origin https://github.com/Uheech/Digital-Toilet`, `git push`)

**지금 바로 터미널 명령어를 실행하여 완료하겠습니다.**
